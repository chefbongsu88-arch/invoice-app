[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile --node-linker=hoisted && find node_modules/react-native-css-interop -name '*.cache' -delete 2>/dev/null; pnpm build:web && pnpm build:server"
